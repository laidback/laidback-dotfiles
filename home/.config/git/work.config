# ~/.config/git/work.config — work identity override
# Included automatically for repos under ~/projects/git.buts.hilti.cloud/
# (matched via [includeIf] in ~/.config/git/config).
# Managed by laidback-dotfiles (stowed from home/.config/git/work.config).

[user]
  name  = Lukas Ciszewski
  email = Lukas.Ciszewski@Hilti.com
