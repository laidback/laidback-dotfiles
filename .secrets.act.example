# act local CI secrets — copy to .secrets.act and fill in values.
# .secrets.act is gitignored. This template is safe to commit.

# GitHub token for actions that pull from GHCR / call the GitHub API.
# A classic PAT with `read:packages` is enough for `act -W .github/workflows/ci.yml`.
GITHUB_TOKEN=ghp_replace_me
